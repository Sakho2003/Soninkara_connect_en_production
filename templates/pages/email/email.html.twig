{% extends 'themes_visiteur/base.html.twig' %}

{% block title %}Réinitialisation de votre mot de passe{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mt-5 mb-4">Salut !</h1>

        <p class="mb-4">Pour réinitialiser votre mot de passe, veuillez visiter le lien suivant :</p>

        <a href="{{ url('reset_password', {token: resetToken.token}) }}" class="btn btn-primary mb-4">{{ url('reset_password', {token: resetToken.token}) }}</a>

        <p class="mb-4">Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

        <p>Cordialement !</p>
    </div>
{% endblock %}
